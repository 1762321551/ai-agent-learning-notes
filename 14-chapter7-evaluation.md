# 第七章：评估、失败归因与可重复的改进

日期：2026-09-26。来源：[第七章原文](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter7.md)。这是阅读笔记与后续实验设计，AI 辅助整理；本次未运行 τ²-bench、AndroidWorld、模型排名或多模型性能测试。原书成绩不作为我的实测结果。

## 从“运行完成”到“任务成功”

前面的库存实验让我实际遇到了本章最重要的问题：请求正常返回、工具正常执行、任务最终答对，是三个不同判断。一个系统可以正常结束，却没有交付需要的缺料清单。

评估应先写出用户要完成什么，再规定能够证明完成的状态和输出。例如库存任务需要检查物料集合、缺口天数、排序和依据；有写入动作的任务还要检查数据库实际变化与禁止修改的范围。

所以我不会只看 Agent 最后的“已完成”，也不会只看 HTTP 200。完成声明必须能与实际状态和执行证据对应。

## 偶尔成功与稳定成功

Pass@k 关注同一任务尝试 k 次是否至少成功一次；本章的 Pass^k 关注 k 次是否全部成功。前者适合探索候选方案，后者更接近反复交付的可靠性。

若单次成功概率为 p，且各次相互独立、概率相同，至少一次成功的概率是 `1-(1-p)^k`，全部成功的概率是 `p^k`。这是假设下的概率关系，不是把少量观察直接变成可靠性的证明。

例如 p=0.8、k=3，两者分别是 99.2% 和 51.2%。能从三份答案里挑出一份正确答案，不代表系统连续处理三次都可靠。实际报告要说明重试预算、挑选方法、失败记录以及是否重置环境。对会产生副作用的工具，还不能无条件重复执行。

## 可重复评估需要准备什么

| 要素 | 库存任务的例子 |
|---|---|
| 任务数据 | 固定物料、明确筛选与排序要求 |
| 初始环境 | 每次相同的数据快照与权限 |
| 工具接口 | 一致的查询定义、分页和错误语义 |
| 验证标准 | 独立计算缺口，核对集合、数值和顺序 |
| 执行协议 | 固定轮次、时间、调用上限和终止条件 |

还应记录版本、提示词、模型参数与运行时间。比较两个方案时，尽量只改变目标变量，并对同一任务做配对比较。如果工具内直接实现了全部解题逻辑，得到的成绩也不能当作模型规划能力。

## 评分程序也可能错

之前 E 组把表格外“已排除”的料号误计入答案，造成错误失败；C 组某些料号又来自调用记录而非最终清单，造成表面上较高的 precision。这两件事说明，抽取到字符串不等于理解了它在答案中的作用。

我会先定义评分范围：哪些表格是结果，哪些是说明，哪些内容不能计入候选。保留原评分与修正后的评分，并解释规则变化。修订验证器之后应统一重评全部方案，不能只修改有利于某一组的结果。

还要用反例检查评分：正确表格加排除说明是否仍通过？调用日志里的料号是否被误算？数值正确但漏行是否失败？只写“全部成功”能否骗过验证器？这些边界比让评分器重复确认同一份正确答案更有用。

## 开放式答案怎样评

结构明确的数值、数据库状态、文件存在性优先使用确定性检查。报告质量、对话体验等较开放的项目，可以用 Rubric 指导人工或模型评审。

我会把要求拆成可核验条目：事实有来源，关键问题都回答，推理与数据一致，不编造未返回结果，完成声明有证据。重要错误可以设为否决条件；避免用“理解深刻”“写得专业”这种难以一致执行的描述。

LLM-as-a-Judge 仍可能偏爱长答案、固定位置或自己的表达风格。可以匿名比较、交换候选顺序、检查不同长度的答案，并用人工标注的边界案例校准。模型给出的分数和理由也属于待验证输出。

## 失败归因：找最早的可证据化偏离

我会沿着原始要求、观察、决策、调用、返回、状态核验和最终答复回看轨迹。先找到第一处不可接受的行为，再判断后续错误是否由它引起。

例如查询工具返回被隐藏之后，模型拿不到库存，却继续猜缺料清单，问题不应仅标成“最终数值错误”。应检查缺失信息是否被识别、是否请求补充、是否宣称了没有依据的事实。

但第一处偏离不一定就是深层根因。输入截断、环境故障、工具错误和验证器缺陷都可能造成相似症状。归因应区分已观察的事实与仍待验证的解释，避免从结果直接猜模型内部原因。

## 把失败变成下一轮的测试

端到端测试看整项任务最后能否完成。轨迹前缀测试则冻结某个决策前的上下文和状态，只检查下一步行动，适合低成本验证具体修复。

可以把“查库存时用户改变项目”“旧偏好与当前要求冲突”“工具尚未返回就准备报告”等场景保存成前缀用例。答案应该是可接受动作集合：先查询最新数据或明确报告等待，都可能合理；编造结果则明确禁止。

只测前缀会漏掉后续执行问题，只测端到端又难定位病因。两种评估应配合使用。模型替换与组件消融也解决不同问题：前者固定运行框架看模型差异，后者固定其他条件关闭组件看系统贡献；单个小样本不能直接证明瓶颈来源。

## 成本与延迟也要按任务算

除了成功率，我会记录每次调用的输入、输出与缓存用量，工具费用，墙钟时间和重试次数。便宜的请求如果经常失败，成功交付一项任务的成本可能更高。

用户看到第一个字的时间与整个任务完成时间不同；输出速度也不等于工具执行速度。尾部延迟需要足够多的样本，少量运行不适合包装成稳定的 p95 或 p99 结论。

第四章按需发现降低了累计输入，但增加了请求轮数且没有降低实测耗时，正好说明成本、速度和质量要分别比较。缓存与历史压缩还可能相互影响，不能把两个节省百分比直接相加。

## 本章实验与我的后续路线

| 原书实验 | 我需要学会的事 |
|---|---|
| 7-1～7-2 | 读懂任务状态、执行协议，亲自验证任务是否可做 |
| 7-3～7-5 | 设计结构评分，比较记忆方案，校准音频评审 |
| 7-6～7-7 | 依据失败轨迹归因，构建下一步动作的边界测试 |
| 7-8～7-9 | 理解配对排名与模型行为策略，控制比较条件 |
| 7-10～7-11 | 记录全链路成本和多维性能，而非只看单价 |

以上为阅读整理，本次未执行。最适合我下一步补做的是库存回归集：增加临界相等、缺失字段、分页遗漏、旧数据和任务中途修正等用例；先验证评分器，再比较系统版本。

我的收获是：可靠的评估要能回答“哪里成功、哪里失败、证据是什么、下一轮只改什么”。保存失败比只展示最佳答案更有利于持续学习。
