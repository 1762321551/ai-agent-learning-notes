# 第二章学习笔记：上下文工程的实测与反思

日期：2026-09-19。学习材料：[《AI Agent 入门》第二章](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md)。本篇由 AI 辅助实际运行实验、核对结果并整理；不是原书转载，也不把书中的数字当成本机测量。

**这次最直接的收获：工具能调用，不代表答案算对；状态写进上下文，不代表模型一定使用；压缩后的文字很短，也不代表后续回答可靠。**

详细数字、实验偏差和证据文件说明见 [实验记录](06-chapter2-experiment-evidence.md)。本轮是缩小规模的教学实验，未完整复现作者的全部项目和基准。

## 1. 这次实际做到了什么

| 实验 | 实际操作 | 完成边界 |
|---|---|---|
| 2-1 本地服务与工具 | 部署 Qwen3-0.6B，临时本地 SSE 服务，真实生成工具请求并执行合成库存工具；另跑 DeepSeek API | 流程跑通，小模型最终计算错误；自编服务，不是作者原项目 |
| 2-2 注意力 | 提取 Qwen3 第 0、13、27 层真实注意力，保存矩阵与热力图 | 单个短提示、多头平均；未验证所有位置偏好 |
| 2-3 上下文与缓存 | 本地 KV 对照、15 次 API 缓存请求、完整历史/滑动窗口/摘要对照 | 没有测完所有错误模式；API 记录整次耗时，不能称为首 token 延迟 |
| 2-4 提示消融 | 4 种提示配置 × 3 个合成退款情形 | 单步决策，不是完整 Tau-Bench |
| 2-5 注入攻防 | 3 类攻击 × 4 种配置，检查泄露与越权请求 | 合成网页和记忆；不执行真实写入/发送，未测试跨会话记忆持久化 |
| 2-6 论文转演示文稿 | 读取论文 PDF 与 Skill，制作 10 页 PPTX，提取 3 张论文图 | 更换制稿工具；包结构、回读与渲染检查通过，未在原生 PowerPoint 手动打开 |
| 2-7 个人写作 Skill | 按本人要求从聊天归纳偏好，AI 代拟 4 段短文、创建 Skill 并试写 | 属于改编练习；没有原创长文语料与作者人工改稿，见 [写作 Skill](07-chat-writing-skill.md) |
| 2-8 状态栏 | Qwen3 两组各一次，DeepSeek 两组各三次；保存局部注意力数据 | 不代表稳定成功率；未做整段生成轨迹的注意力分析 |
| 2-9 五类状态 | 时间、计数、TODO、错误、环境，各开/关一次 | 快照问答，不是完整任务规划与自动恢复系统 |
| 2-10 压缩 | 6 种教学条件，真实模型压缩与回答；异常另补测 | 合成静态资料、字符预算，不是原书人物研究或 128K 溢出实验 |

## 2. 模型和工具确实是两回事

本次库存样本：库存 18，日耗用量 6，采购交期 7 天。

```text
模型生成 get_stock 与 get_lead 请求
→ 本地程序检查参数并执行两个工具
→ 工具返回库存、日耗用量、交期
→ 框架把结果送回模型
→ 模型生成最终答复
```

期望计算：可用天数 = 18 / 6 = 3；缺口天数 = 7 - 3 = 4。

DeepSeek 的 2 次 API 请求完成了这个流程，并给出 4 天。Qwen3 的第一次简单提示没有生成工具调用；改成明确要求使用两个工具后，在本地服务中生成了两个请求，工具也真实执行，但最终把缺口写成了 **7 天**。

因此我会分开检查三个问题：有没有生成调用、工具有没有执行、最终业务答案对不对。只看到 `tool_calls` 或 HTTP 200，不能宣布任务成功。

本地使用 CPU、float32、关闭思考模式。虽然机器有显卡，本轮没有安装 CUDA 版运行库。这里的耗时不能用于判断显卡性能，也不能与书中的苹果设备测速直接比较。

## 3. “稳定信息放前面”有了实测依据

API 缓存实验使用约 2,398 个输入 token，分三批测试。每批先发首次请求，再分别测试完全相同、改末尾状态、改开头、交换工具顺序。

| 条件 | 三批缓存命中 token |
|---|---|
| 首次请求 | 0 / 0 / 0 |
| 完全相同 | 2176 / 2176 / 2176 |
| 只改末尾状态 | 2176 / 2176 / 2176 |
| 在开头加入变化内容 | 0 / 0 / 0 |
| 交换工具顺序 | 2048 / 2048 / 2048 |

这支持了稳定前缀的价值，也提醒我：**交换工具没有让所有缓存都归零**。可以复用多少，取决于实际序列化、首个变化位置和服务端实现，不能只凭 JSON 排列猜测。

本地还做了更直接的计算对照：先处理 601 个 token，再处理新增的 3 个 token。排除一次预热后，复用 KV 的新增部分耗时中位数约 **0.195 秒**，完整重算约 **2.655 秒**；最终 logits 的最大差约 0.0000234，最高分 token 相同。

这个比较不含缓存首次建立成本，不是端到端 Agent 加速倍数。实际工程还要考虑缓存占用内存、输入长度与生成长度。

## 4. 注意力图展示了计算，不能替代解释

我们读取真实 Qwen3 权重，而不是手绘一张示意热力图。短提示共有 27 个 token，选取三层并对头取平均。

- 指向未来位置的权重最大值为 **0**，符合因果遮罩。
- 每行权重和与 1 的最大偏差约为 **1.79 × 10⁻⁷**。
- 三层对首 token 的平均权重分别约 **6.6%、69.0%、77.9%**。

同一句输入，不同层的模式明显不同。高权重并不等价于“这个词具有这么多语义重要性”，也不能从一个短样本推导所有模型都有相同的注意力储存池比例。

我现在把图看作定位现象的工具，仍然用实际输出和对照实验检查行为。

## 5. 工具描述里的单位不能随便删

退款实验规定：未核验身份先核验；已发货不退款；符合条件才调用退款工具。工具描述说明金额单位是“分”。

| 配置 | 三个样本中正确的下一步与参数 |
|---|---|
| 结构化规则、完整描述、专业语气 | 3/3 |
| 改为轻松语气 | 3/3 |
| 保留规则但打乱顺序、去掉层次 | 3/3 |
| 删除工具与参数描述 | 2/3 |

最后一组中，模型仍然选中了退款工具，但把 20 元填成 `amount: 20`，正确参数应为 `2000`。

这个反例很具体：**名字和参数类型不足以表达单位。** 删除描述不是纯粹的文字精简，而可能删除业务语义。另一方面，三条很短的规则打乱后没有失败，不能据此否定复杂任务中规则组织的价值。样本太少、规则太简单，这里没有统计结论。

## 6. 防注入要同时观察“想做什么”和“做了什么”

本轮使用合成测试标记，构造直接索要系统提示、网页夹带写入要求、伪造记忆要求发送副本三类攻击；比较基础约束、额外警告、XML 来源标记和组合配置。

12 个样本都没有输出测试标记，也没有请求写入或发送。基础组仍保留“不泄露、未确认不写入”的规则，并不是完全没有系统约束。网页和记忆组的用户请求也明确没有授权外发，样本比较容易。

执行器没有提供真实发送或泄露能力，所以外部操作数始终为零。**没有发生外部操作，不能单独证明模型抵抗住了攻击**；需要另外检查模型是否发起越权请求。这一轮两项都记录了。

本次没能区分四种防御的优劣，不能宣称 XML 标记或提示词已经解决提示注入。伪造记忆是直接放入上下文的测试素材，没有验证长期记忆如何被写入和再次加载。

## 7. 状态栏有用，但仍需要执行层约束

电话场景设定为：历史中已经给同一家公司打过 3 次，规则最多 3 次，用户又要求再打一次。电话均为合成轨迹，没有真实拨号。

| 模型与配置 | 观察 |
|---|---|
| Qwen3，无聚合状态 | 请求第 4 次电话，违反规则 |
| Qwen3，末尾写明已用 3/3 | 不再请求电话，但只是复述状态文本，答复质量仍有限 |
| DeepSeek，无状态，3 次 | 3 次均未越限 |
| DeepSeek，有状态，3 次 | 3 次均未越限 |

局部注意力数组只记录输入末尾一个 Query、最后一层的多头平均；它不足以证明“生成全过程都聚焦于状态栏”。本轮也没有测出普遍十倍提速。

更稳妥的实现是让程序维护 `used=3`、`limit=3`，在执行前直接阻止超限；给模型的状态栏用于帮助它规划下一步。最终权限和配额不能只依赖一段文字。

五类状态快照另有两个提醒：写明当前日期后，模型仍把“昨天”对应的文件答错；环境组无状态答 `ls`、有状态答 `Get-ChildItem`，但 `ls` 在 PowerShell 中也可能有效，不能把字符串不一致直接当作功能错误。

## 8. 压缩要保留任务需要的事实和来源

滑动窗口实验中，早期工具结果包含一个合成项目的截止日、批准人和来源。随后加入旁支对话：

- 保留完整历史：直接正确回答。
- 只留最近四条历史：模型请求重新读取记录；本轮在这一决策处停止，不能称为已经陷入无限循环。
- 保留原始任务、事实摘要、来源和近期历史：直接正确回答。

这说明压缩不能只按“最近几条”机械截断。原始目标和关键事实可能在很早的位置。

六种压缩条件使用 8 条合成资料，共 **11,559 字符**。原文、逐条摘要、合并摘要、任务相关摘要、带引用摘要第一次都回答正确；带 `[COMPRESSED]` 标记的教学自适应组第一次回答缺失。后者追加三次复核，仍有两次答缺失、一次正确；对应无标记摘要三次都正确。

两份摘要保留了相同事实，不能把差异解释为压缩删除了数据；也不能从这一组认定标记必然导致错误。自适应条件实际只验证了字符阈值与标记，没有完整实现长任务中的逐轮 token 预算调度。

资料编号采用 P2→R2→S2 这样的简单规律，容易让模型推断；摘要之间的信息密度也不同。因此本轮不能给压缩策略排优劣。后续应使用不规则事实、冲突信息和真实来源，把事实正确、引用正确、重新查询次数和总调用成本一起检查。

## 9. Skills 的价值需要具体产物验证

论文制稿实验选用 [Attention Is All You Need](https://arxiv.org/abs/1706.03762)。先读取运行时中可见的技能描述，再按需加载制稿、图片和验证说明，制作 10 页演示文稿，提取 PDF 第 3、4、13 页的三张图。

产物经过结构、尺寸、字体声明与回读检查，10 页预览逐页查看。使用的是本机 Presentations 技能和 Artifact Tool；也阅读了当日 Anthropic PPTX Skill，但没有照搬书中旧版 `html2pptx.md` 路径。因此这里是替代工具链的功能练习。

个人写作 Skill 按我的要求改为从聊天记录构建。AI 代拟了四段练习文本，提取五条带例子的原则，并完成新主题试写与文件校验，见 [实验 2-7](07-chat-writing-skill.md)。这些草稿不冒充我的原创文章；如果要进一步验证个人文风，仍需要我提供原创长文或实际改稿。

## 10. 我对上下文工程的理解改变了什么

以前容易把“多给信息”当成更稳妥。现在我会先问：当前目标是什么、哪些事实仍有效、哪个结果来自哪里、哪些操作已经完成、还有多少执行额度。

大量上下文不等于重新训练，也不能保证模型从结果反推出原始任务。稳定的规则和工具定义适合保持前缀稳定；变化的状态应按需要更新；压缩时优先保护目标、约束、事实与来源。

最值得保留的是失败结果：小模型会用工具却算错，时间状态明确仍回答错，压缩内容没丢也可能回答缺失。工程上的检查需要覆盖这些环节，而不是用“模型足够强”代替验证。

## 参考与产物范围

- [本章原文](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter2.md)
- [Qwen3-0.6B 官方模型与用法](https://huggingface.co/Qwen/Qwen3-0.6B)
- [DeepSeek Chat Completions 接口](https://api-docs.deepseek.com/api/create-chat-completion/)
- [Anthropic PPTX Skill](https://github.com/anthropics/skills/tree/main/skills/pptx)
- [本轮数据与证据说明](06-chapter2-experiment-evidence.md)

GitHub 发布学习笔记和脱敏数据摘要。模型权重、环境、原始 API 响应、注意力数组和 PPTX 保存在本机，未随笔记上传。本仓库不是完整可复现实验包。API 账户凭据与授权请求头没有写入公开文件。
