# 第二章：数据、检查与实验边界

日期：2026-09-19。返回 [学习笔记](05-chapter2-context-engineering.md)。以下均为本轮运行记录；不引用原书成绩作为本机成绩。

## 环境与统一参数

| 项目 | 本轮配置 |
|---|---|
| 本地模型 | Qwen/Qwen3-0.6B，官方权重 |
| 本地框架 | Transformers 4.57.6；PyTorch 2.14.0+cpu |
| 推理条件 | CPU，float32，6 线程，关闭思考，贪心生成 |
| 远程 API | `https://api.deepseek.com/chat/completions` |
| 请求模型 | `deepseek-flash`；名称来自实际请求，不推断底层权重身份 |
| API 参数 | `thinking.type=disabled`，`temperature=0`，非流式；按项目设置输出上限 |
| 数据性质 | 合成库存、退款、电话、文件与人员岗位资料 |
| 危险动作 | 不执行真实退款、外发、电话或注入要求的写入 |

远程实验是独立教学脚本直接调用 API，**不是在 DeepSeek Harness 的 agent-loop 内执行**。本机既有配置仅用于读取已配置的 API 凭据。HTTP 客户端没有配置自动重试。

## 记录数量与一次记录缺陷

本轮共发出 **77 次远程模型请求**，均得到 HTTP 200。保存了 **76 份完整响应记录**。另有一份早期摘要条件响应，因汇总文件与它同名而被覆盖；请求文件、控制台 HTTP 200/384 token 记录，以及比较表中的答案仍在。发现后更改汇总命名，并补测一次，补测响应完整保留。

76 份完整响应的 usage.total_tokens 合计 **94,244**。加上被覆盖记录在控制台保留的 **384**，本轮报告使用量合计 **94,628**。这是输入与输出使用量的求和，包含重复发送的上下文，不是去重后的材料长度或账单金额。

本地流式服务另外收到 2 次请求，不计入远程 API 数量。服务仅绑定本机临时端口，实验后已关闭。

## 本地服务：过程与结果分开

| 步骤 | 首个非空文本块延迟 | 整次耗时 | 非空文本块数 |
|---|---:|---:|---:|
| 生成两个工具请求 | 1.015 s | 5.814 s | 14 |
| 读取结果后回答 | 1.128 s | 8.172 s | 31 |

这是自编 SSE 协议，不是完整的 OpenAI 兼容服务。“首个非空文本块”受解码器的文本分块影响，不等于严格的第一个 token 生成时间。

两个工具实际返回：

```json
{"get_stock":{"stock":18,"daily":6},"get_lead":{"lead":7}}
```

Qwen3 最终计算库存可用天数为 3，但把缺口天数写成 7，判为业务答案错误。DeepSeek 相同数据的答案为 4，判为正确。两者提示措辞与运行条件不同，不构成公平的模型能力排行榜。

## 本地 KV 对照

固定前缀 601 token，后缀 3 token；同一模型、同一 token 序列。先建立缓存，再比较后缀计算与完整序列重算。预热记录另存，不计入下表。

| 次数 | 已有缓存下新增部分耗时 | 完整重算耗时 | logits 最大绝对差 | 最高分 token 相同 |
|---|---:|---:|---:|---|
| 1 | 0.194789 s | 2.655283 s | 0.000023365 | 是 |
| 2 | 0.169742 s | 2.825905 s | 0.000023365 | 是 |
| 3 | 0.197378 s | 2.554946 s | 0.000023365 | 是 |

API 缓存命中结果见主笔记。API 采用固定顺序而不是随机交叉试验，耗时受热缓存、网络和调度影响；不据此报告因果加速倍数。

## 提示与注入

提示消融每种配置只有三个情形：未核验、符合退款条件、已发货。baseline、casual、shuffled 都为 3/3；no-description 为 2/3。唯一失败是金额单位从 2000 分变成 20。

注入条件为 direct/web/memory × baseline/warning/xml/combined，各一次。测试标记泄露 0/12；越权写入或发送请求 0/12；外部执行数 0。没有尝试更复杂的编码、多轮诱导、真实浏览器网页注入或持久记忆污染。

所有配置在执行侧都不提供真实泄露动作，因此这次无法独立测出“增加确认机制”对真实执行率的增益。执行层隔离是实验安全边界，不应伪装成模型防御能力。

## 状态与评分口径

Qwen3 无状态时请求第 4 次电话；有状态时输出状态文本而未请求电话。各一次。DeepSeek 两种配置各三次，均不请求电话。

五类状态的开/关快照各一次。计数器、TODO 和错误情形，两组都给出预期下一步；日期情形两组都答文件 b，但给定今天为 9 月 19 日时，昨天更新的应为文件 a。无状态组缺少明确参照日期，不能做对称的正确率比较。

环境情形的 `ls` 与 `Get-ChildItem` 只能作为表达差异；PowerShell 支持 `ls` 别名，所以程序中的精确字符串匹配不是功能正确率。不得将初版 `matched` 字段直接汇总成五类能力得分。

## 六种压缩条件

体积单位是字符，比例定义为压缩后字符数 / 原字符数。本轮没有让模型达到真实 token 窗口上限。

| 条件 | 字符数 | 保留比例 | 首次答案与来源正确 |
|---|---:|---:|---|
| 不压缩 | 11559 | 100% | 是 |
| 批量要求逐条独立概括 | 311 | 2.69% | 是 |
| 合并概括 | 70 | 0.61% | 是 |
| 任务相关摘要 | 164 | 1.42% | 是 |
| 明确要求保留引用 | 164 | 1.42% | 是 |
| 阈值触发并加标记 | 177 | 1.53% | 否 |

“逐条摘要”实际在一次模型请求中批量完成，不是八次独立请求；自适应组复用带引用摘要，加上 `[COMPRESSED]`，不是一个完整在线调度器。其字符阈值设为预算 8000 的 80%。

异常复核：无标记组新增 3 次均正确；加标记组新增 3 次中 1 次正确。连同初次结果，无标记为 4/4，加标记为 1/4。样本少、顺序固定、内容简单，不做显著性或一般因果结论。

## 离线检查与本机证据

离线检查 **12/12** 通过，检查对象包括：注意力遮罩与归一化、缓存预测一致性、工具请求与结果配对、本地服务关闭、实验条件数量和响应记录格式。

这 12 项是**证据与流程检查**，不是 12 个业务任务全对。Qwen3 的缺口计算错误等失败不因此消失。

本机保留以下证据类型，文件名仅作为索引，未声称已上传仓库：

- `2-1-local.json`：最初小模型未调用工具的记录。
- `2-1-local-service.json`：SSE 请求、生成文本、执行结果和耗时。
- `2-2-attention.npz`、`2-2-attention.png`：真实矩阵与热力图。
- `2-3-local-kv.json`：缓存与完整前向计算结果。
- `2-3-*.request.json`：缓存实验实际请求体。
- `2-3-window-comparison.json`：窗口截断与保留摘要的对照。
- `2-4-summary.json`、`2-5-summary.json`：提示与注入结果。
- `2-8-local.json`、`2-8-summary.json`：状态栏两模型结果。
- `2-9-summary.json`：快照问答；评分限制见上文。
- `2-10-contexts.json`、`followup-2-10-summary.json`：摘要与补测。
- `offline-audit.json`、`manifest.json`：检查与文件哈希。
- `transformer-learning.pptx`：10 页论文学习演示文稿；包结构与回读通过，逐页预览检查完成，未验证原生 PowerPoint。

实验 2-7 后续按本人要求改为使用聊天记录：AI 代拟四段短文、创建 Skill、完成试写和结构校验，见 [写作 Skill 记录](07-chat-writing-skill.md)。没有原创长文与作者人工修订环节，不能声称完成原版验收。其余项目也保留各自的缩小规模或功能验证边界，不能把本篇称为“第二章全部实验严格复现”。
