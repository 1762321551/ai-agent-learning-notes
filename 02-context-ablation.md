# 02｜实验 1-1：上下文消融

[返回目录](README.md) · [下一篇：研究任务](03-research-workflow.md)

## 1. 实验问题

同一个模型面对同一个库存任务，如果工具定义、工具结果或较早历史缺失，会发生什么？

实验不是让模型“假装忘记”，而是在实际请求中移除或替换相应内容。每次使用新会话，任务、合成数据、系统提示及其他参数保持一致。

## 2. 任务与控制条件

- 框架：DeepSeek Harness `0.1.5-rc.2`。
- 请求记录中的 provider/model：`deepseek-official / deepseek-flash`。这里记录的是请求路由和模型 ID，不独立证明服务商背后的权重版本。
- 10 个合成物料，包含库存、日耗用量、采购交期等字段。
- 仅提供三个只读工具：物料列表、库存查询、采购查询。
- 库存与采购工具单次最多查询 5 个料号。
- 受测 Agent 没有终端、通用文件、网络或子 Agent 等替代取数渠道。
- 每组最多 8 次模型调用，各实际运行一次。

判定规则：

```text
库存可用天数 = 库存 / 日耗用量
缺口天数 = 采购交期 - 库存可用天数
仅保留缺口天数 > 0 的物料
按缺口天数降序，并列按料号升序
```

独立程序计算标准答案，不提前提供给受测模型。

## 3. 对照设计

| 组 | 唯一主要变化 | 验证点 |
|---|---|---|
| A | 无消融 | 完整上下文基线 |
| B | 去掉请求中的工具定义 | HTTP 请求体没有 tools 字段 |
| C | 工具照常执行，结果正文替换为“结果已隐藏” | 保留调用与结果 ID 的合法配对 |
| D | 计划移除历史 reasoning | 本实验实现标记不适用，没有真实运行 |
| E | 只保留原任务与最近一轮完整工具交互等必要上下文 | 较早交互确实从请求中移除 |

D 的结论限于本次版本、适配器和实现方式。不能据此宣称所有模型接口都无法独立处理历史 reasoning。

## 4. 第一轮真实结果

| 组 | 模型调用 | 工具执行 | 重复调用组数 | 运行结束 | 人工复核任务结果 |
|---|---:|---:|---:|---|---|
| A | 3 | 5 | 0 | completed | 成功，6 个物料及排序、数值均正确 |
| B | 1 | 0 | 0 | completed | 未完成，无真实查询 |
| C | 5 | 6 | 1 | completed | 未完成，说明无法取得数据，未编造清单 |
| D | 0 | — | — | 未测试 | 不适用 |
| E | 3 | 5 | 0 | completed | 成功，6 个物料及排序、数值均正确 |

B/C/E 共 9 次 HTTP 请求，加上 A 为 12 次；已有记录显示均返回 HTTP 200。正常响应和正常结束仅说明运行过程，并不能代替任务验收。

标准答案：

| 排名 | 料号 | 缺口天数 |
|---|---|---:|
| 1 | MAT-1005 | 4 |
| 2 | MAT-1009 | 4 |
| 3 | MAT-1003 | 3 |
| 4 | MAT-1008 | 3 |
| 5 | MAT-1001 | 2 |
| 6 | MAT-1006 | 1 |

库存可用天数恰好等于交期的 MAT-1004 不应入选。

## 5. E 组为什么还能成功

E 最终请求保留四条查询结果：两批库存与日耗用量、两批采购交期，覆盖全部 10 个物料。被删除的是更早的物料列表交互，而计算所需数值仍然完整。

因此这次观察支持的是：删除冗余历史不一定影响结果。它不支持“历史无用”，也没有直接测试删除不可恢复的关键数值后会怎样。

## 6. 自动评分也会出错

首次评分把 E 回答里“已排除”的料号算成错误入选项。将判定范围改为表格后修复了这一问题，但 C 的“尝试调用参数表”又被误认为答案表。

所以 C 的自动 precision=1 没有任务成功意义。最终采用人工复核：C 没有给出缺料清单，任务未完成。原始评分保留，重评与人工结论分别记录。

我的经验是：评分器需要同时理解“答案”和“解释”的区别，不能只匹配料号是否出现。

## 7. 证据与限制

本地依据包括 `experiment-summary-round1.md`、`human-review-round1.md`、各组 `metrics.json`、`final-answer.txt`、`wire/*.json`。公开笔记没有上传这些原始会话记录。

- 每组一次，不构成统计结论或通用成功率。
- C 的真实运行没有耗尽上限；不能把 mock 的循环行为当成真实结果。
- 各组耗时还受输出长度、缓存及网络等因素影响，不作因果比较。
- 这是合成库存任务，不覆盖跨会话记忆或真实业务验收。
- D 未测试，不能评价移除 reasoning 的作用。

**我的结论：工具定义、真实反馈和必要历史分别解决不同问题；必须检查请求和执行记录，才能解释模型表现。**
