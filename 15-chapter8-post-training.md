# 第八章：模型后训练——从修提示词到改变模型行为

日期：2026-09-26。学习来源：[第八章原文](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter8.md)。本文是阅读笔记与个人理解，AI 辅助整理；本次未执行训练、蒸馏或本章基准实验。文中的库存案例是教学设计，不是新增实测结果。

## 与前面七章怎样连起来

前面的学习主要是在模型之外改系统：提供合适的上下文、查询知识库、设计工具、管理事件、验证结果。第八章讨论另一条路线：更新模型权重，让它更经常表现出我们需要的知识和行为。

这也回应了我之前关于“样本足够多，偏差可以很小”的想法。数据增加可能有帮助，但必须继续问：样本覆盖了哪些情形，监督目标是什么，测试题是否真的不同于训练题？训练集回答接近标准答案，不能直接证明模型在新物料、新规则和异常工具返回下仍可靠。

## 先区分四个训练环节

| 环节 | 主要数据与目标 | 我用来理解它的库存例子 |
|---|---|---|
| 预训练 | 从大规模数据学习 token 之间的规律 | 获得语言、数字、代码等通用基础 |
| Mid-training | 在目标领域继续语言建模 | 学习供应链术语、文档结构和领域关系 |
| SFT：监督微调 | 学习输入对应的高质量输出示范 | 学会按工具协议查询、报告缺失字段并输出表格 |
| RL：强化学习 | 生成候选行为，依据奖励优化策略 | 在可重置环境里尝试查询与恢复，强化真实成功的路径 |

这是一张职责地图，不是所有项目必须依次完成的固定流程。已有模型的能力、数据与预算决定下一步。

SFT 通常以回答 token 为监督目标，属于语言建模式训练。RL 则利用模型生成的答案或轨迹及其奖励，调整行为概率。两者都可以改变模型，但获得信号的方式不同。

## 库存事实应该放在哪里

今日库存、最新采购交期和访问权限需要更新、追溯与控制，我会继续放在数据库和工具中。把某一天的全部库存训练进权重，会让更新和纠错更困难。

长期稳定的领域概念可能适合领域训练；查询格式和异常处理可以用示范学习；库存计算和写入权限仍可由程序保障。RAG、上下文学习和后训练可以配合使用，各自处理不同问题。

例如模型不理解“采购交期”，可能是领域知识缺口；懂概念却不会传 `material_id`，可能是接口行为问题；工具正常但漏查第二页，可能是规划或状态跟踪问题。先定位缺口，才能选择训练方法。

## 为什么不能把 SFT 和 RL 简化成“背诵与理解”

SFT 直接提高示范输出的概率，数据太单一时容易学到表面模板；多样且有代表性的示范也能帮助泛化。RL 强化获得奖励的行为，有机会发现示范之外的策略，但也可能利用奖励漏洞或过拟合模拟环境。

因此原文用“SFT 记忆、RL 泛化”描述部分受控实验的倾向，不能据此断言 SFT 永远不泛化、RL 永远更好。

对库存任务，只用固定十个物料示范，模型可能记住清单。更合理的训练与测试应改变物料数量、字段顺序、缺口分布、分页方式和错误类型，并使用未见过的数据核验。

## RL 为什么需要一个能提供差异的起点

一次 rollout 是按当前策略完整尝试任务的一段过程。若所有尝试都无法解析，或全部只能得到相同的失败奖励，学习信号就很弱。

GRPO 的一个关键思路是在同题候选组内比较奖励；仅用组内相对奖励时，全部得零分会失去这部分优势差异。它不说明所有优化项都必然为零，但说明奖励设计没有告诉系统哪条轨迹更值得强化。

我会先测模型是否偶尔能成功：如果它能完成任务但成功概率不高，训练有可强化的行为；如果合理采样仍几乎无成功，要检查基础能力、格式、任务难度与可达的中间目标。少量样本没有成功也不是“绝对做不到”的证明。

缺知识时考虑补领域数据，接口不会表达时先稳定格式，能局部推进时考虑课程或部分反馈。不是只增加 RL 训练时长就能解决所有问题。

## 奖励必须对应真实目标

前面库存评分器曾把排除说明里的料号误算入答案。这种错误若用于训练，模型就会被错误信号推动。第七章检查评分器，第八章又把它变成了训练前提。

| 奖励设计 | 可能诱导的错误 | 更合适的检查 |
|---|---|---|
| 奖励“完成”字样 | 只宣称完成 | 核对最终清单与环境状态 |
| 奖励调用次数少 | 跳过必要查询 | 同时检查完整性与调用成本 |
| 奖励格式正确 | 合法 JSON 内填假数据 | 分开验证格式与事实 |
| 只奖励测试通过 | 修改断言或绕过检查 | 保护验证器并审查实际修改 |

终点奖励检查任务最后是否办成；过程信号检查中间动作或可达进展。过程信号能帮助长任务归因，但也可能把模型绑在预设路径上。罚分过强还可能诱导什么都不做，因此要保留任务目标并提供可行的合规路径。

原文介绍的路径惩罚思想，适合能明确判定的违规动作。它仍不能替代交付完整性、语义理解和权限执行层。训练过“不跨项目查询”，不等于数据库可以省掉项目权限检查。

## 多轮训练还要学习错误后的恢复

只收集成功示范，可能让学生主要见到顺利路径。部署时一次传参错误会带它进入陌生状态，后续也可能继续出错。

我会补充工具超时、分页不完整、字段缺失、用户中途改变项目等场景，明确合理的恢复行为。模拟器需要忠实表达这些状态；如果它总给整齐的成功返回，训练出来的策略可能依赖这种便利。

训练集、调参集和最终测试集应隔离。物料号不同但任务模板、答案模式完全相同，也可能形成泄漏或过于容易的测试，不能只按文件名判断独立性。

## 蒸馏：向教师学习什么

离线蒸馏可以把教师生成并验证过的答案或轨迹交给学生学习。在轨蒸馏则由学生生成它实际会到达的前缀，再由教师在这些位置提供监督，更贴近学生自己的错误分布。

这里的“在轨”关注状态由谁采样，不是教师必须等于学生。教师也可能有偏差，学生可能继承它的错误与冗长习惯。蒸馏是否划算，需要把教师推理、环境交互和训练成本一起计算。

自蒸馏可以让同一模型在教师角色中看到已验证答案或更多信息，而学生只看正常输入。特权信息只能出现在训练侧；若测试或部署输入也泄露答案，成绩就失去意义。

## DPO 和 LoRA 分别解决什么

DPO 使用同一输入下较好与较差的回答偏好对，直接优化相对偏好。例如把“没检查就宣称完成”列为 rejected，把“先核验再如实报告”列为 chosen。它依赖可靠的偏好数据，不能代替真实环境验证，也不等同于在线 RL 交互。

LoRA 是参数高效更新的方法：冻结主要权重，训练附加的低秩参数。它与 SFT、RL 是不同层面的选择。使用 LoRA 不会自动解决数据质量、泛化和遗忘问题；rank、学习率与训练层的选择需要在目标模型上验证，不能把某个实验配方当作通用保证。

## 训练前后怎样验收

我会固定相同工具与执行协议，记录训练前后：格式有效率、真实任务成功率、未见任务表现、工具错误后的恢复、违规动作、成本和延迟。同时运行通用能力保留集，检查领域提升是否伴随其他能力下降。

训练 loss 下降，只证明对训练目标拟合得更好。窗口能接收长输入，也不证明模型在长上下文中能正确检索和规划。应分别测不同长度、信息位置、干扰程度和任务类型。

RL 还需要检查采样策略与训练计算的一致性，记录策略版本和概率差异。数值实现或过旧采样数据造成的错位，也可能表现为训练不稳定。

## 本章实验路线与当前范围

| 原书实验 | 学习重点 |
|---|---|
| 8-1～8-2 | 用小型环境理解奖励、策略和传统 RL 与 LLM Agent 的区别 |
| 8-3～8-5 | 预训练、视觉扩展、继续预训练及能力保留 |
| 8-6～8-9 | 语音、多语言与提示词/思维链蒸馏的示范数据 |
| 8-10～8-11 | 思考成本控制、SFT/RL 的分布变化对照 |
| 8-12～8-15 | 导航、机器人与工具使用的多轮训练 |
| 8-16 | 结果奖励与可验证路径信号 |
| 8-17～8-19 | 把早停、作用域与精确复制失败转成后训练数据 |

本次以上实验均未执行。对我最有价值的起点是把已有库存与工具错误转成小型、可重置的评估环境，先验证评分和数据，再决定是否值得微调。稳定事实、输出协议与行动策略需要分别诊断，不能把所有问题都归入“模型不够强”。

我的收获是：后训练让模型更倾向于某种行为，而行为是否值得学习，首先由数据、环境和验证信号决定。系统里的明确权限、事实查询和最终验收仍然重要。
