# 第五章：代码能力、运行框架与结果验证

日期：2026-09-22。学习来源：[第五章](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter5.md)。AI 辅助实验与撰写。覆盖范围见[实验附录](12-chapter45-experiment-evidence.md)。

## 代码把模型的想法变成可检查的过程

第三章讨论如何取回正确资料，第四章讨论如何调用工具，第五章进一步强调：代码本身可以成为计算、规则、数据处理和界面生成的通用能力。

但“代码能精确运行”不等于“生成的代码准确表达了任务”。模型可能误解条件、漏掉边界、传错参数；代码会忠实执行这些错误。运行框架需要把生成、执行、观察和修正连起来。

## 数学与逻辑对照：没有预设代码一定胜出

本次六道合成数学题分别涉及模运算、路径计数、数位和、多项式系数、约数和子集计数。每题同一模型分别直接回答与生成 Python，代码经过检查后在本地运行，并与独立计算真值比较。

| 任务 | 直接回答 | 生成代码后执行 |
|---|---|---|
| 六道数学题 | 五题给出正确最终答案；一题输出截断 | 六题结果正确 |
| 三道骑士与无赖题 | 两题正确；一题额外引入不存在的 C | 两题生成代码并算对；一题返回文字而非代码 |

数学真值依次为 49、442、670、2907、216、9364。直接回答子集题被 1,800 token 上限截断，因此差异包含输出预算影响，不能简单归因为推理能力。

逻辑题中，代码组失败的一题，文字答案其实给出了正确的 A/B 身份，但它没有生成可执行程序，所以仍未完成代码组任务。直接组在同一题正确识别 A/B 后，又添加了不存在的 C。评估必须检查所要求的输出对象与过程。

这不是 AIME 或 K&K 完整数据集，也没有统计显著性结论。代码执行是经检查的本地进程，不是经过安全认证的沙箱。

## 日志解析：先测试，再替换

让模型生成新格式解析器，处理 `时间|级别|模块|消息`。特别要求消息中出现竖线时不能被错误拆开。

六个测试覆盖正常记录、消息里的分隔符、错误日期、未知级别和空模块，全部通过。随后将进程内的解析函数替换为通过测试的版本，新格式从无法识别变成正确解析。

这里只实现了进程内替换，没有做浏览器日志可视化、完整部署或运行时回滚。生成器返回了 Markdown 代码围栏，执行前只去掉围栏，没有悄悄修改业务逻辑。

## 日志诊断：让复现证明修复

合成轨迹中，A 项目的 M1 库存为 18，B 项目的 M1 为 72，但缓存键只使用料号，导致 B 读到 18。模型根据轨迹 T1/T2 定位隔离问题，并生成包含项目与料号的元组键。

修复后重放五次读取，全部得到预期数值。另加入 `AB+C` 与 `A+BC`，避免直接拼接字符串造成新的键碰撞。

这比仅输出“建议优化缓存”更能说明修复有效。不过它仍是合成系统，不是生产根因报告；没有替用户向外部项目创建 Issue。

## SQL：看起来合理，也必须让数据库运行

创建 SQLite 教学数据库，含五个合成员工和四笔工资记录。模型生成三条 SELECT，在只读模式下实际执行：

- 在职人数：engineering=1、sales=1。
- 截至指定日期平均任职：248.5 天。
- 八月工资：engineering=220003 分、sales=90003 分。

用例包含未来入职、已离职和当天离职员工；离职日期按不包含当天处理。金额保持整数分，不在文字生成阶段重新加总。

这是 SQLite 改编，不是原书 PostgreSQL 与 artifact 数据传输方案的完整对照。

## 动态表单：页面出现不等于功能完成

初版模型把大量篇幅花在 CSS，输出被截断，HTML 不完整。要求更精简后得到完整页面，但仍没有遵守“最多 100 行”的约束，这也算格式遵循问题。

浏览器实际测试确认：单程隐藏返程日期并输出 null；往返显示返程字段；返程早于出发时拒绝提交；合法往返输出预期 JSON。测试仅使用合成行程，没有真实预订。

这个实验让我理解，界面需要交互验证，不能只看截图或代码。尚未覆盖空白城市、所有浏览器与辅助技术等边界。

## 权限不能只靠模型承诺

四个本地数据网关测试分别尝试跨租户更新、跳过状态转换、超预算工资和合法更新，前三项被拒绝，合法项成功；拒绝后数据保持不变。

这体现了把规则放到稳定执行路径的价值。但 Python 网关与 SQLite 并不能证明生成代码无法绕过权限：若同一进程仍能直接访问数据库，边界就可能被绕过。本次没有实现 PostgreSQL 行级安全或独立安全域。

## 创建 Agent：评分程序差一点掩盖了真问题

分别让模型从零生成循环与参考简短模板生成循环。初版测试只检查消息配对、步数上限和最终文字，两组都通过。

加强测试后发现，从零版本把整个参数字典作为一个位置参数传给工具，而约定的工具要求关键字参数。由于初版假工具没有检查参数类型，错误被放过了。严格检查实际传参和返回值后，从零版本失败，模板版本通过。

这里暴露的是接口约定和测试质量问题。提示词原先也没有充分说明 callable 的参数约定，因此不能把失败全部归因于模型。模板只是一段短循环，不是原书高质量完整 Agent 范例；测试使用确定性模拟模型响应，也没有验证真实多轮 API Agent。

## 中断恢复也要检查含义

使用两个手工截断的 JSON 前缀，比较续写与整段重发，两种方式都得到合法且语义正确的城市与金额。没有复现原书的真实流式断线、跨厂商接管和错误率，所以不能宣称续写更安全或更省成本。

我的主要收获是：代码和工具给了系统可检查的中间步骤，但正确性仍要由合适的真值、边界用例和实际执行来确认。尤其不能让一个返回固定“成功”的测试替真实工具作证。

参考：[第五章实验入口](https://github.com/bojieli/ai-agent-book/blob/main/chapter5/README.md)、[原书验收记录](https://github.com/bojieli/ai-agent-book/blob/main/chapter5/EXPERIMENT_LEDGER.md)。
