# 实验 2-7：根据聊天记录构建学习笔记写作 Skill

日期：2026-09-19。返回 [第二章笔记](05-chapter2-context-engineering.md)。

这项实验按本人要求，改成从已有聊天记录提取表达偏好，再由 AI 代拟短文。**不是从 3–5 篇本人原创长文归纳风格，也没有完成作者人工改稿闭环。** 以下文本是 AI 辅助草稿，不用于声称辨识出了作者稳定、独有的文风。

## 1. 使用的聊天依据

只采用与学习和表达有关的片段：

- “因为库存查询是独立于模型外的，模型是模型工具是工具”——倾向把不同环节分清楚。
- “模型忘记原始任务，而去通过结果猜任务，会偏离原始方向”——关心目标保留与偏差。
- “全部库存、日耗用量和采购交期”——愿意落到具体字段讨论。
- “稳定信息”——偏好简洁、直接的回答。

这些只是聊天中的线索，不能证明本人在所有场景下都喜欢短文。技术报告、复现实验和正式作业需要足够细节时，仍应写清楚。

## 2. 四段代拟练习文本

以下四段由 AI 根据聊天与本轮结果起草，**不是历史上由本人写出的文章**。

### 练习一：工具和模型

模型会选工具，但库存数据不是模型自己查出来的。程序要接住调用请求，检查参数，执行查询，再把结果送回去。所以我检查实验时，不能只看模型说了什么，还要看工具到底执行了没有。

### 练习二：原始任务

库存、日耗用量和交期都在，不代表任务就完整了。这些数据可以用来算缺口，也可以用来排采购优先级。原始要求被删掉以后，模型可能猜中，也可能换一个方向。因此要压缩旧记录时，我会保留任务、筛选条件和排序要求。

### 练习三：缓存

之前我只知道稳定信息应该放前面。这次看到重复请求命中 2176 个 token，改开头后命中变成 0，才有了直接印象。不过这是这组请求的结果，不能说所有模型改一个字都会整段失效。

### 练习四：验证结果

本地小模型把两个工具都调通了，但最后把缺口算成 7 天，正确结果是 4 天。如果只检查工具调用，这个任务就会被误判为成功。以后我会把流程检查和答案检查分开。

## 3. 可加载的 SKILL.md

本机已生成并校验同内容的 `SKILL.md`。如需使用，可将以下内容保存为一个独立技能目录中的 `SKILL.md`；本次没有自动安装到全局技能目录。

```markdown
---
name: chat-grounded-learning-notes
description: 根据本次用户的聊天偏好撰写或修改中文 AI Agent 学习笔记、实验心得；强调具体证据和表达清楚。不自动用于正式论文、法律文件或其他作者的文风模仿。
---

# 聊天记录驱动的学习笔记

这些偏好来自有限聊天，由用户授权归纳，仍需后续修订。不得把 AI 代拟样文说成用户原创，也不能把一次实验结果概括成模型的普遍能力。

## 写作原则

1. 先写观察，再解释含义。
   避免：“本实验全面彰显上下文工程的巨大价值。”
   改为：“重复请求命中2176个token；改开头后命中为0。本组结果支持保持前缀稳定。”

2. 把模型请求、工具执行和任务结果分开。
   避免：“工具调用成功，所以任务成功。”
   改为：“两个工具已经执行，但缺口答成7天，正确值为4天。”

3. 用具体对象和字段解释概念。
   避免：“保存高价值上下文。”
   改为：“保留原始任务、料号、库存、日耗用量、交期和来源。”

4. 用正常口吻说明限制，不用绝对承诺。
   避免：“状态栏彻底解决模型遗忘。”
   改为：“这次加状态后没有请求第四次电话，但样本只有一次。”

5. 正文保持简洁，证据可以放表格或附录。
   避免：正文连续堆叠所有日志字段。
   改为：先写结果和关键数字，再链接实验记录。

## 作用域与例外

用于本人的中文学习笔记草稿和实验心得。遇到详细报告、复现实验或用户要求完整过程时，不为追求简短删除必要步骤。引用、数据、单位和限定条件优先于文风。

## 使用与检查

先确认原始材料中的事实，再按上述原则起草。交付前核对数字、单位、来源和未完成项。仅根据用户实际修改更新偏好；不把 AI 自改当作用户反馈。发布到外部平台仍需当前任务已有授权。
```

## 4. 新主题试写：状态栏为什么不能代替程序校验

修改前（为对照故意写成空泛草稿，AI 起草）：

> 状态栏赋予了 Agent 强大的自我感知能力，能够全面提升决策质量，确保系统高效可靠运行。

按 Skill 修改后（同样由 AI 修改，未声称是作者人工改稿）：

> 状态栏可以提醒模型已经打过三次电话，但提醒不等于限制。模型仍可能提出第四次调用。我的做法是由程序保存次数，执行前检查上限；状态栏负责让模型知道为什么不能再打，以及还能做什么。这样就把“模型看见了状态”和“系统守住了额度”分开了。

检查结果：有具体对象，分清提醒与执行，没有承诺永不出错，也没有捏造新的实验数字。技能文件通过结构校验。上述检查验证了草稿是否遵守规则，**没有证明它与本人原创长文的风格相似**。

后续真正的改进依据，应是本人对这段文字的修改：哪些句子会保留，哪些表达会换掉，再把重复出现的偏好写回 Skill。
