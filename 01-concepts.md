# 01｜核心概念与工程理解

[返回目录](README.md) · [下一篇：上下文实验](02-context-ablation.md)

## 1. 我怎样理解 Agent

Agent 接收目标，选择行动，通过工具获取信息或改变环境，再根据反馈决定下一步。一次文本回答可能是其中一个环节，而不是完整任务。

理解系统时，我把它拆成三部分：

| 部分 | 负责什么 | 例子 |
|---|---|---|
| 模型 | 理解需求、规划与决策 | 决定先查询库存还是采购交期 |
| 上下文 | 为当前决策提供信息 | 任务、约束、历史、工具结果 |
| 工具 | 提供可执行的接口 | 查询数据、搜索网页、运行程序 |

这些组件与外部环境交互。数据库、网页和磁盘文件有自己的真实状态，不能因为模型声称更新了它们，就认为状态已经改变。

## 2. ReAct：行动之后必须观察

我把运行过程记成：

```text
接收目标 → 决定下一步 → 调用工具 → 观察返回结果
                    ↑                  │
                    └──── 调整策略 ────┘
                          ↓
                     检查完成条件
```

例如库存任务需要先获取料号，再取得库存与交期，最后计算缺口。若查询失败，下一步应由错误反馈决定；若查询成功但数据不可见，模型仍然没有计算依据。

## 3. Harness 的作用

Harness 是围绕模型的运行与管理层。我关注五项职责：

1. 组织上下文，让模型看到任务需要的信息。
2. 提供明确的工具接口。
3. 约束可执行操作的权限和范围。
4. 验证实际结果是否满足要求。
5. 在失败时重试、回退、停止或请求人工处理。

模型变强，可以减少某些提示词补丁或格式修补，但真实系统的权限、状态和验收仍需要工程实现。Harness 也不应无限膨胀：先用简单方案，针对已观察到的失败增加必要机制。

## 4. 工作流与自主 Agent

| 方式 | 路径由谁决定 | 适合的任务 |
|---|---|---|
| 工作流 | 预先编写的程序和条件 | 固定审批、受规则约束的交易步骤 |
| 自主 Agent | 模型结合执行反馈 | 调研、排错、开放式编程 |
| 混合 | 固定边界内允许动态选择 | 客服方案比较与规则化提交 |

选择依据是任务是否需要动态决策，而不是架构看起来是否先进。一次调用可以解决的问题，没有必要引入复杂的多 Agent 系统。

## 5. 上下文增长为什么昂贵

若每轮新增约 k 个 token，且每轮都重新输入全部历史，n 轮的累计历史输入量约为：

```text
k + 2k + ... + nk = k × n × (n + 1) / 2
```

这是近似平方增长的输入量模型，不等于实际计费或延迟公式。前缀缓存、工具输出长度和模型实现都会影响实际成本。

可采用结构化状态、摘要、按需检索、工具输出裁剪降低增长。压缩的验收标准应是关键事实仍能被找到，而不是只看节省了多少 token。

## 6. 三种“学会了”需要分开

- 当前上下文带来临场适应，不代表模型参数发生改变。
- 把经验保存为文档、技能或程序，可以让后续任务重新利用。
- 训练更新模型参数，是另一种更新机制。

在实验中记录经验并不等于训练了模型。更准确的说法是：改进了下一次任务可使用的信息与流程。
