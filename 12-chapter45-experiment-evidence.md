# 第四、五章：实验覆盖与证据边界

日期：2026-09-22。配套：[第四章笔记](10-chapter4-tools.md)、[第五章笔记](11-chapter5-code-and-harness.md)。

## 本次运行概况

DeepSeek API `deepseek-flash`，thinking 关闭，temperature=0。47 次真实请求全部 HTTP 200，usage 合计 72,769 tokens；其中直接回答子集题、初版动态表单两次输出被截断。HTTP 成功不代表内容完整或任务成功。

另执行本地 Python、SQLite、MCP stdio 客户端/服务端，以及 Edge 浏览器表单交互。模型生成代码在检查后运行，不能把 AST 检查与超时视为安全沙箱证明。数据均为合成教学数据；不修改真实业务系统。

## 第四章覆盖

| 实验 | 本次状态 | 证据与差距 |
|---|---|---|
| 4-1 主动发现 | 缩小改编已执行 | 123 个合成 schema，同一道库存题两组各三次；两组均正确；输入 tokens 降低，耗时未降低。非小模型、非向量发现、非真实跨领域任务 |
| 4-2 感知 MCP | 部分已执行 | 真实 SDK 初始化、列工具和限量文件读取；未覆盖多模态、私有数据和全部公开 API |
| 4-3 多模态三路线 | 本次未执行 | 没有同一图文材料、同一问题的三路线模型对照 |
| 4-4 执行 MCP | 部分已执行 | 合法文件写入、语法拒绝、路径越界拒绝；未做完整终端沙箱、桌面、邮件和日历体系 |
| 4-5 协作工具 | 仅本地状态演示 | 建立并读取 pending 审核请求；无真实人工批准、通知或多 Agent 协作 |

## 第五章覆盖

| 实验 | 本次状态 | 证据与差距 |
|---|---|---|
| 5-1 厂商接管 | 本次未执行 | 没有用多厂商真实接口切换；未用同厂商模拟冒充 |
| 5-2 中断续写 | 缩小机制对照 | 两个手工 JSON 前缀，续写/重发均正确；不是实际流式断网 |
| 5-3 数学代码 | 缩小对照 | 六题代码正确；直接五题正确、一题截断；非 AIME，未做显著性分析 |
| 5-4 逻辑代码 | 缩小对照 | 三题直接与代码任务各两题符合要求；非原 K&K 数据集，未使用 python-constraint |
| 5-5 代码规则 | 仅确定性演示 | 四类订单按服务端真值判定取消；未跑小模型双臂比较 |
| 5-6 论文到 PPT | 本次未重跑 | 第二章有论文制稿产物，但不能作为本章单/双 Agent 对照证据 |
| 5-7 论文到视频 | 本次未执行 | 无真实配音、逐页视听审查与成片同步验证 |
| 5-8 视频编辑 | 本次未执行 | 无原视频与视觉定位、剪辑复核链路 |
| 5-9 CAD 对生成模型 | 本次未执行 | 无两种真实三维产物及同规格尺寸测量 |
| 5-10 自适应解析 | 部分已执行 | 模型生成、六测试、进程内替换；无浏览器日志前端和完整部署 |
| 5-11 日志诊断 | 部分已执行 | 合成缓存串项目错误、模型诊断、生成修复、五例重放；无生产轨迹与外部 Issue |
| 5-12 动态表单 | 缩小实测 | 真实生成 HTML，Edge 实测单程/往返与日期验证；初版截断保留，修复后可用 |
| 5-13 ERP SQL | 缩小改编 | SQLite 三查询与预期一致；无 PostgreSQL、无 artifact 传输成本对照 |
| 5-14 界面定制 | 本次未执行 | 动态表单不等于 React/FastAPI 多轮热加载，未混算完成 |
| 5-15 权限数据对象 | 部分机制演示 | 三项非法写入拒绝、一项合法通过；SQLite+Python 网关非独立隔离边界 |
| 5-16 Agent 生成 | 缩小生成与协议测试 | 模板/从零各生成一个循环；严格参数检查后模板通过、从零失败；没有真实新 Agent 多轮 API 验收 |

## 关键评分说明

工具发现：全量组共 6 次模型请求、50,727 prompt_tokens、12.01 秒；发现组 9 次、5,438 prompt_tokens、13.57 秒。累计输入下降约 89.3%。这不是总费用降幅，也不是普遍延迟结论。发现器按英文关键词选择候选，未匹配时返回固定库存与采购工具。

数学真值：模运算 49；避开指定点的路径 442；四位以内数位和 18 的计数 670；多项式系数 2907；约数 216；子集计数 9364。六个生成程序均执行出正确结果。直接组子集题未给最终答案，不能算错误数值，但算未完成。

逻辑真值：l1=(0,1)，l2=(0,1,0)，l3={(0,1,1),(1,0,0)}。直接 l1 引入多余的 C，代码 l1 输出文字不可执行。代码组其余两题使用穷举而非约束求解库。

MCP：6 次 call_tool，加初始化与 list_tools。pending 状态从未伪装成人工批准。只有五个注册工具，没有实现整章工具平台。

日志：解析测试全部通过后才替换函数。缓存修复由料号键改成项目/料号元组；五次重放覆盖项目隔离和字符串拼接碰撞。全部发生在合成系统。

Agent：初版弱测试曾错误通过从零版本；追加语义测试检查工具实际收到 `material == M1` 后揭示参数约定不一致。原弱测试和追加失败结果都保留，不覆盖历史。此结果不能推出模板生成普遍优于从零生成。

表单：初版截断；后续独立修复请求生成完整 HTML。实际浏览器确认单程 returnDate=null，往返 2026-10-05 可提交，早于出发日 2026-10-01 的 2026-09-30 被拒绝。修复稿仍超过提示要求的 100 行，功能通过不等于完全遵循格式。

## 证据保存与公开内容

本地保留原文快照、原书验收清单、请求正文、响应、usage、生成代码、执行输出、数据库查询、MCP 收据、浏览器观察记录和 SHA-256 清单。认证头与密钥不写入这些公开笔记。

主要证据文件：`math-logic-verification.json`、`mcp.json`、`parser-activation.json`、`cache-replay.json`、`sql-verification.json`、`agent-semantic-verification.json`、`browser-form-verification.json`。这些名称说明本地证据类别，不代表已上传全部文件。

GitHub 发布的是 Markdown 学习记录，不是独立可运行实验包。本次没有将 21 项全部标为完成，也没有把原书作者的正式结果计入本机实测。后续完整复现应补齐上表未执行项与各项正式验收。

参考：[第四章验收清单](https://github.com/bojieli/ai-agent-book/blob/main/chapter4/EXPERIMENT_LEDGER.md)、[第五章验收清单](https://github.com/bojieli/ai-agent-book/blob/main/chapter5/EXPERIMENT_LEDGER.md)。
