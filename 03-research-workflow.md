# 03｜实验 1-3：研究任务的功能复现

[返回目录](README.md) · [下一篇：思考与心得](04-reflections.md)

## 1. 目标与验证范围

观察 Agent 能否搜索资料、取得数据、编写代码计算，并根据结果继续查证。使用现有 Harness 的工具实现流程，不把它表述成原书中特定模型或提供商托管研究循环的原样复现。

固定城市集合为：曼谷、河内、万象、金边、内比都、吉隆坡、新加坡、雅加达、马尼拉、斯里巴加湾市。这里是指定的十座城市，不对当前成员国或首都归属作额外断言。

## 2. 实际取数路径

| 能力 | 实际执行方式 | 结果 |
|---|---|---|
| 搜索 | Harness 的 web_search | 得到来源清单 |
| 网页抓取 | web_fetch | 本机域名解析与公网地址检查冲突，读取失败 |
| 结构化坐标 | pwsh 中运行 Node 脚本，读取 MediaWiki API、Nominatim | 得到并保存 JSON |
| 补充网页 | 本地 Node 脚本读取页面 | 得到部分网页正文 |
| 计算 | 本地 Python | 计算全部 45 对距离 |

上述路径是本次报告记载的实际行为。工具安全检查阻断不应被泛化成“换一个工具即可绕过”；正式系统需要确认替代路径具有相同的授权范围及安全边界。

检索摘要、网页正文、结构化 API 数据是不同证据类型。搜索工具可用，不代表正文读取工具也通过了验证。

## 3. 坐标与计算口径

主来源统一取英文维基百科条目的主要坐标，第二来源使用 OpenStreetMap Nominatim。二者可能选取不同的城市代表点，因此坐标差异不等于计算错误。

采用 Haversine 公式，角度先转弧度，地球半径 R=6371.0088 km：

```text
a = sin²(Δφ/2) + cosφ₁ × cosφ₂ × sin²(Δλ/2)
d = 2R × atan2(√a, √(1-a))
```

十座城市的无重复配对数为 10×9/2=45。这里算的是球面大圆距离，不是道路里程、实际航程或旅行耗时。

## 4. 结果

| 排名 | 城市对 | 主来源大圆距离/km |
|---|---|---:|
| 1 | 吉隆坡—新加坡 | 315.3 |
| 2 | 河内—万象 | 476.1 |
| 3 | 曼谷—万象 | 522.6 |
| 4 | 曼谷—金边 | 538.1 |
| 5 | 万象—内比都 | 713.1 |

第二来源中，吉隆坡—新加坡约为 309.2 km；本次两套数据的最近五对排序一致。代表点差异会改变数值，但没有改变本次“最近一对”的判断。这不是对所有可能坐标选择的数学证明。

补充来源代表点组合给出的最近一对距离约为 306.4～315.7 km。报告同时比较了球面与椭球口径；展示统一保留一位小数，完整精度保留在本地数据文件。

## 5. 如何复核

已有离线验证输出记录 14/14 项通过，包括：两来源各有十城、集合一致、45 对唯一且无自配对、计算结果与已保存表格一致、最近一对及前五对匹配。

重算与四位小数 CSV 的最大差约 4.6 厘米，在舍入容差内。它表明同一批数据能被重复计算，不证明城市代表点有厘米级准确度。

第二来源核验检查的是结论对坐标选择是否敏感；离线复算检查的是计算能否重复。两者不能相互替代。

## 6. 我观察到的研究循环

先搜索并尝试读取；读取受阻后采用报告记载的替代取数路径；取得主数据后计算；发现不同来源的新加坡代表点差异较大，再补充查证；最后离线复算并解释口径。

现有报告把检索整理成阶段，不能将这种归纳当成逐事件精确时间线。要严格还原每次决策顺序，还需要完整会话事件日志。

## 7. 没有验证的部分

- web_fetch 本身未恢复。
- 没有验证提供商托管的完整搜索与代码执行循环。
- 会话渲染信息显示 deepseek-flash，但当时未取得可独立核对的请求日志，模型身份保留这一限制。
- 未取得可信道路里程，不用估计值补齐。
- 本次整理读取已有报告与验证输出，没有再次联网取数或运行计算。

本地证据类型：`report.md`、`evidence.md`、`out/verify-offline.txt`、坐标 JSON、45 对距离 CSV、原始 API 响应和脚本。本公开仓库仅整理结论及方法。

## 来源入口

- [MediaWiki 坐标 API](https://www.mediawiki.org/wiki/Extension:GeoData#API)
- [OpenStreetMap Nominatim](https://nominatim.org/)
- [吉隆坡条目](https://en.wikipedia.org/wiki/Kuala_Lumpur)
- [新加坡条目](https://en.wikipedia.org/wiki/Singapore)

这些是本次学习报告涉及的公开来源入口；在线页面可能继续更新，实验数值以当时保存的数据为准。
