# 04｜十道思考题与学习心得

[返回目录](README.md)

以下按第一章思考方向整理个人回答，不作为唯一标准答案。

## 1. 只能增强一项能力，选什么

先定位瓶颈：缺资料补上下文，缺执行接口补工具，信息与工具齐全但判断不稳才优先考虑模型。B、C 两组说明，更强模型不能凭空取得未提供的实验数据。

## 2. 怎样降低历史读取成本

用结构化状态记录进度，精简工具结果，压缩旧历史并保留按需检索入口。先证明关键事实仍可恢复，再评价节约了多少 token。缓存优化也应与逻辑上下文裁剪分开讨论。

## 3. 模型自主性与 Harness 是否矛盾

不矛盾。决策可以更多交给模型，而权限、资源预算、状态持久化与结果验证仍需要可靠实现。应逐步撤掉已不必要的适配代码，将工程精力放在真实失败和业务约束上。

## 4. 如何发现空转

检测重复工具参数、连续同类错误、长期没有新增有效信息、反复修改同一产物却不接近验收。设置时间与调用上限，触发后换策略或报告阻塞。本次 C 组有重复调用，但没有耗尽预算。

## 5. 从知识、行动、策略看 Harness

知识来自任务、历史、检索和工具结果；行动由查询、文件和执行接口提供；策略体现在选择步骤和处理失败。我的改进优先级是补全证据记录、明确验收标准、改善工具错误信息，而不是先增加更多 Agent。

## 6. 航班延误客服如何设计

使用混合方案。Agent 理解偏好并比较替代航班；身份、订单权限、退改条件、金额和提交步骤由确定性规则控制。最终改签执行应遵循用户确认及业务规则。

## 7. 工具风险如何随参数变化

工具名相同不代表风险相同。删除缓存与删除系统文件的影响不同。程序应解析真实路径、操作范围、可恢复性及权限，再决定放行、确认或拒绝，不能只依赖模型自己的口头判断。

## 8. 什么时候限制动作空间

固定业务边界、不可逆操作及高错误成本场景适合受限工具。例如只提供受控退款接口，而不是任意数据库写入。库存实验限制为三个只读工具，也排除了替代取数对实验的干扰。

## 9. 用户不在线时怎样处理

保存进度，暂停需要确认的动作，继续与该决定无关的安全工作，并清楚说明所需决策。超时按事先约定处理；沉默不是授权，模糊指令也不应被扩大解释。

## 10. 哪些原则更持久

验证真实状态更持久；特定格式修补可能被模型或 API 能力替代。外部系统仍可能超时或部分失败，因此“模型说完成”始终需要与可验证结果对应。

## 学习心得

这次学习改变了我评价 AI 的方式。以前我容易被流畅、完整的回答吸引，现在更关心它有没有取得真实数据、有没有执行工具，以及结果是否满足事先定义的目标。

最有启发的是三个差别：行动意图与真实执行的差别、运行结束与任务成功的差别、计算可重复与数据口径正确的差别。它们都需要证据，不能仅凭回答的语气判断。

上下文实验让我理解了信息的作用，也提醒我不要过度解读单次观察。研究实验则让我看到，Agent 的价值还包括处理工具反馈、发现数据差异并继续查证。评分误识别进一步说明，自动化检查本身也会出错。

今后的实践中，我会先明确目标、工具权限和完成标准，从最小可运行方案开始，再根据真实失败补充机制。每次实验保存原始结果，将观察、解释和未验证事项分开记录。

## 下一步学习计划

- 用更多独立运行检查上下文实验结果是否稳定。
- 设计独立任务，让关键数据分布于多轮交互，进一步研究历史删除。
- 使评分输出具有明确结构，同时保留人工复核依据。
- 在授权和安全边界内排查网页抓取工具问题。
- 学习模型请求日志、工具调度与状态持久化如何协同。

以上是后续计划，不是已经完成的验证。
