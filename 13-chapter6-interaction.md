# 第六章：交互、事件与持续变化的环境

日期：2026-09-26。来源：[第六章原文](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter6.md)。这篇是阅读笔记和个人理解，AI 辅助整理；本次未运行本章实验。此前操作经历只用于帮助理解，不计作本章基准结果。

## 我对 Agent 的理解又增加了一层

之前讨论库存时，我说“模型是模型，工具是工具”。这一章让我看到两者之间还有时间问题：工具运行期间，用户会补充条件，数据库会变化，网页也可能跳转。系统需要记住任务进展，并决定新信息什么时候进入下一步决策。

观察不仅是读取更多内容，也包括听声音、看屏幕，以及接收外部主动到达的消息。动作不仅是增加工具，还包括让任务在后台运行、停止正在执行的操作、处理新要求后恢复旧任务。这些变化最终都要落实到可观察的状态。

例如查库存时，我先要求找出缺料物料，查询尚未结束又补充“只看项目 A”。系统应保存这一约束，等查询结果到达后按新范围筛选；若补充的是“取消查询”，执行器还需要真正处理取消。

## 异步：先分清依赖，再安排并发

| 新事件 | 可以怎样处理 | 必须检查什么 |
|---|---|---|
| 用户要求停止 | 中断当前流程，尝试取消工具 | 工具是否真的停下，是否已有副作用 |
| 补充筛选条件 | 保存并在合适边界合并处理 | 是否同时保留所有有效条件 |
| 独立的简单问题 | 单独处理后继续主任务 | 是否把两个任务的数据混在一起 |
| 后台结果到达 | 按任务标识交回对应流程 | 是否过期、重复或属于已取消任务 |

这里的“可以”是设计选择，不能只凭一句话的形式决定策略。一个看似独立的查询也可能依赖主任务结果。

启动成功、正在运行、执行完成和任务成功是不同状态。耗时工具可以先返回任务 ID，之后再返回结果。任务 ID 证明系统接受了工作，不证明工作已经做完。

对要求工具调用与结果配对的同步接口，运行框架需要维护合法消息顺序；必要时用明确的未完成状态说明后台任务，随后带着来源和任务 ID 交回真实结果。不能把占位消息当成业务数据。使用异步程序也不自动证明模型接口支持原生异步；原文中的具体产品能力属于版本相关信息，实际使用前还要核对接口文档。

## 打断与恢复需要保存哪些东西

至少要保留原始目标、最新约束、已完成步骤、未完成任务、已发生的外部修改以及结果归属。新的用户要求可能改变计划，但已经发送的消息或已经提交的修改不会自动撤销。

这和我们发布 GitHub 笔记的经历很接近：浏览器连接中断后，应该先确认哪些文件已经提交，再补剩余内容。若只根据最后一条报错重做全部步骤，就可能重复发布或覆盖已有成果。

我会给库存任务保存一张小状态表：当前项目、数据时间、筛选条件、分页进度、待返回的查询和最终是否核验。这样恢复时不用让模型从零猜任务。

## 语音：文字正确仍可能损失信息

级联语音系统把语音检测、识别、语言处理和语音合成串起来，便于替换组件和定位问题，但各阶段都会增加等待。转成纯文字也可能丢掉停顿、情绪或环境声。

直接处理音频的模型有机会保留更多信息，但“能输入输出音频”不等于“能持续边听边说”。按轮次交互和全双工交互还需要区分。分段送入越来越长的音频，也不等于底层模型具备真正的流式编码能力。

假如我口述一个料号，在中间停顿后纠正最后两位，系统应该等有效输入确定后再进行依赖它的修改。推测执行可以帮助提前准备，但对尚未确认的识别结果应保留修正路径。

评价语音体验时，我会分别看听清内容、判断是否说完、首次可听响应、打断后停止播放，以及后续是否遵守修正后的要求。

## 屏幕操作：观察—动作—再观察

网页的 DOM 和无障碍树能提供按钮、输入框等语义信息；纯视觉定位则从截图寻找目标。结构信息有助于选择元素，但可能遗漏画布、遮挡或动态内容。坐标操作也会受窗口尺寸和页面变化影响。

关键是每次重要动作后重新读取现实：填入了什么、是否出现确认框、保存是否成功、页面是否跳转。点击动作成功，只说明输入被发送，不能证明目标状态成立。

我们更新学习仓库时，提交后的页面与公开文件内容提供了两种证据：前者确认提交发生，后者确认发布的文字完整。这个经验与本章闭环思想相符，但不是原书 Computer Use 或 browser-use 实验的正式复现。

## 机器人：模型规划和实时控制各自负责什么

高层规划决定对象、目标和任务顺序，技能层执行抓取、放置，底层控制处理运动与安全。高层推理的延迟不能替代实时控制回路。

世界模型可以预测候选动作的结果，帮助选择动作；预测仍需环境反馈确认。模拟环境中抓取成功，不证明真实机械臂在摩擦、标定误差和遮挡下也能成功。对硬件任务，这和“编译通过不等于台架验收”是同一个证据原则。

## 实验怎么读，下一步怎么做

| 原书实验 | 学习目标 | 我会关注的验收证据 |
|---|---|---|
| 6-1～6-3 | 事件队列、并行、中断及原生交互对照 | 任务归属、取消状态、更新约束、恢复后的实际结果 |
| 6-4～6-7 | 级联、音频前缀、端到端音频与合成控制 | 实际音频、延迟分段、转录损失和听感核验 |
| 6-8～6-9 | 桌面与浏览器操作 | 操作前后观察、实际页面状态、失败恢复 |
| 6-10～6-14 | 遥操作、自主控制、模拟及跨环境测试 | 真机和模拟分别报告，检查最终状态与安全条件 |

本次这些实验均未执行。最适合我先练的是一个可取消的后台库存查询：加入两条条件更新和一次取消，检查执行记录与最终答复是否一致。之后再增加语音或视觉输入，避免一次改动多个因素。

我的收获是：Agent 与用户协作的可靠性，不只取决于某一轮想得对不对，还取决于新信息如何进入、旧任务如何恢复，以及每个动作是否被现实确认。
