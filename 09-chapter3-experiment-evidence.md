# 第三章实验记录：结果、方法与证据边界

日期：2026-09-19。配套[学习笔记](08-chapter3-memory-and-rag.md)。本页记录真实执行结果；所有业务数据均为合成数据。

## 1. 实际环境和调用

- 生成模型：直接调用 DeepSeek API，请求模型为 `deepseek-flash`，关闭 thinking，temperature=0。
- 云端共 120 次请求，全部 HTTP 200，记录 usage 合计 46,399 tokens；未出现输出长度截断。包含最初对照及明确标记的后续诊断。
- 本地脱敏：Qwen3-0.6B，Transformers，CPU；没有使用 Ollama 服务。
- 本地向量：`sentence-transformers/all-MiniLM-L6-v2`，384 维、归一化。
- 神经重排：`cross-encoder/ms-marco-MiniLM-L-6-v2`。
- ANN：Annoy 1.17.3 与 Faiss 1.15.1 的 HNSW。不是 hnswlib；其预编译包不可用后改用 Faiss。
- 沿用上一章实验环境，新增检索依赖。全部数据处理与索引在本机完成，云端只接收合成会话和合成资料。

120 次请求成功仅表示通信成功，不表示 120 个任务都成功。密钥从本机已有配置读取，不写进笔记、请求正文或公开仓库。

## 2. 十二项覆盖情况

| 原书实验 | 本次实际完成 | 与完整验收的差距 |
|---|---|---|
| 3-1 三层记忆评估 | 三层各 2 个合成用例、逐会话替换记忆 | 不是原书 60 例；无独立 LLM 评委 |
| 3-2 四种记忆格式 | 同模型同案例四格式；共 24 个回答、72 次调用 | 通过提示词控制格式，未运行原项目全部实现或框架对照 |
| 3-3 本地脱敏 | 三条日志，正则/模型/并集合并对照 | 非 Ollama；仅四处敏感真值；位置由匹配原文计算 |
| 3-4 ANN | 真实向量，同数据比较 Annoy/HNSW 与精确检索 | 2,000 条小规模；一次 CPU 测量；未测 RSS |
| 3-5 BM25 | 自写分词、倒排、TF/IDF 与排序，手算复核 | 小语料；中文只做单字切分 |
| 3-6 混合检索 | 稀疏、稠密、两种融合、真实交叉编码器 | 12 文档、8 查询；英文模型不适合作通用中文结论 |
| 3-7 结构化索引 | LLM 生成小型层级摘要和关系图并问答 | 非完整 RAPTOR/GraphRAG；无 Intel 手册规模实验 |
| 3-8 Agentic RAG | 同一库存问题，一次检索与模型自主再检索 | 合成库存任务；非中文司法 QA；未做多样本重复 |
| 3-9 检索记忆 | 四条合成历史，通过搜索工具查询有效合同 | 不是原书三层完整套件；无固定窗口长历史测试 |
| 3-10 上下文前缀 | 六文档真实生成前缀，BM25/向量双索引对照 | 人为构造身份缺失；无大规模噪声库 |
| 3-11 双层记忆 | 普通/前缀/前缀加卡片；追加依赖保留诊断 | 四历史记录、少量问题；无完整独立评委评估 |
| 3-12 结构化知识 | 因素发现、24 例提取、聚类、6 个留出特征样本 | 非 CAIL2018；不含真实司法建议或因子重要性验证 |

以上状态均为“机制改编已执行、原书完整验收未完成”。没有把小样本检查写成原项目 `validation/latest.json` 的全部通过。

## 3. 记忆与脱敏

每格式包含六个案例，每案例两次记忆更新和一次回答。最终回答只见最后记忆，不见原始历史。

| 格式 | 规则通过 | 全流程 tokens | 累计请求耗时 | 平均记忆字符 |
|---|---:|---:|---:|---:|
| Simple | 6/6 | 3,589 | 24.47 秒 | 115 |
| Enhanced | 6/6 | 3,358 | 23.01 秒 | 104 |
| JSON | 6/6 | 4,679 | 24.44 秒 | 235.83 |
| Advanced | 6/6 | 9,652 | 29.13 秒 | 839.33 |

规则检查预设的实体、数值、状态关键词以及澄清表达。它不能代替逐句语义评审，不声称覆盖所有幻觉。每个条件只跑一次，耗时受网络与缓存影响，不作因果或显著性结论。

脱敏采用敏感字符串精确匹配，三个样本共四处真值：

| 方法 | TP / FP / FN | Precision | Recall | 业务字段完整保留的样本 |
|---|---|---:|---:|---:|
| 正则 | 2 / 0 / 2 | 100% | 50% | 3/3 |
| 本地模型 | 2 / 5 / 2 | 28.57% | 50% | 1/3 |
| 正则与模型并集 | 2 / 5 / 2 | 28.57% | 50% | 1/3 |

模型还把部分 confidence 输出成字符串，未满足严格数值类型。实现记录其原始输出，未把无效字段悄悄修复成合规结果。并集只是最简单组合策略，不能代表所有混合脱敏方案。没有复现原文提到的 95% 以上召回率。

## 4. 检索指标

ANN 参数：Annoy 10 棵树、search_k=1000；HNSW M=16、efConstruction=80、efSearch=32。两者使用同一批 384 维归一化向量。精确内积搜索提供 top-10 真值，65 个查询。

| 后端 | Recall@10 | 构建 | 平均单查询 | 索引文件大小 |
|---|---:|---:|---:|---:|
| Annoy | 86.92% | 0.0944 秒 | 0.2735 ms | 3,331,296 字节 |
| Faiss HNSW | 90.92% | 0.1079 秒 | 0.0530 ms | 3,359,994 字节 |

计时不含向量编码；两边最终均逐个查询。索引文件大小不是进程内存。Annoy 已保存索引后追加被拒绝；HNSW 追加新向量成功，其自身查询返回新编号 2000。未测试长期增量退化、删除、多并发或持久化恢复。

BM25 使用 `log(1+(N-df+0.5)/(df+0.5))`，k1=1.5、b=0.75。教材手算单独采用不加 1 的原公式，避免两个 IDF 口径混淆。手算结果：doc1=3.665182，doc7=1.684287，doc5=1.223775，doc3=0.823935。

12 文档、8 查询，每查询恰有一个标注相关文档。最终 Recall@3 / MRR / nDCG@3：BM25 为 0.625 / 0.625 / 0.625；稠密、RRF、归一化融合、重排均为 0.875 / 0.803 / 0.813。重排只处理融合前六个候选。RRF 常数为 60。相同汇总指标不代表逐条排名完全一致。

初版错误把 BM25 零分文档也当成候选；已修正并重算，以上只报告最终口径。源数据、预期标签和各路排名保存在本地，未用模型自评代替相关性标签。

## 5. 答案与证据复核

- 库存一次检索：缺 S1/L1，回答无法判断。迭代检索：得到 P1/S1/L1，计算 18÷6=3 天、7−3=4 天，并引用依据。
- 有效合同：普通块能回忆 9 天，却不能恢复供应商；生成前缀后正确返回 South、C-29、9 天，并排除未接受报价。
- 公司资料：普通块 BM25 Top-1 为 1/6、向量 0/6；带前缀两者均 6/6。前缀由模型读原文生成，不是直接手写答案。
- 项目准备：普通块检索失败；第一版前缀漏掉依赖关系，也未发现风险；全局概览卡片补足信息后，指出证件在开工前过期。
- 后续诊断：只修正前缀使其保留“Vega 依赖 CERT-71”，无需卡片也成功提醒。该对照在发现错误后追加，单独保存，没有替换原失败记录。
- 结构化索引：两种生成索引均支持四跳链路；图回答引用 e3/e4/e6/e7 更细。答案也有多余的“缺少直接边”等限制描述，不能把全部解释视为必要或完全正确。
- 故障原型：24 例抽取为电压/温度/松动状态；三类各 8 例。六个未参与拟合的数值样本按预期分到低电压、过热、松动类。抽取字段由脚本预设，恰好与模型发现一致，不声称自动动态生成了完整 schema。

## 6. 本地证据与公开范围

本地保留请求正文、原始响应、usage、检索查询与返回记录、阶段记忆、模型原始脱敏输出、向量排序、聚类输入和结果；生成 SHA-256 清单。认证头不保存。

关键文件类别：`memory-summary.json`、`pii.json`、`ann.json`、`retrieval.json`、`contextual-ranking.json`、各 `*-trace.json`、`factor-clusters.json`、`audit-summary.json` 和 `manifest.json`。

GitHub 本次只发布中文 Markdown 学习记录，不上传真实用户记忆、凭据、模型权重、全部日志或源码。因此这些文件名是本地证据说明，不是公开下载承诺；本仓库仍不是独立可运行的复现实验包。

原书标准：[实验验收清单](https://github.com/bojieli/ai-agent-book/blob/main/chapter3/EXPERIMENT_LEDGER.md)。后续严格复现应使用原案例、固定版本与独立评委，并补齐本页表格中的差距。
