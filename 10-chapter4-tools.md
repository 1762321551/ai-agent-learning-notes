# 第四章：工具设计与真正的执行边界

日期：2026-09-22。学习来源：[第四章](https://github.com/bojieli/ai-agent-book/blob/main/book/chapter4.md)。AI 辅助实验与撰写。完整覆盖情况见[实验附录](12-chapter45-experiment-evidence.md)。

## 从“模型是模型，工具是工具”继续往下想

以前讨论库存时，我说库存查询独立于模型。现在可以把这句话展开：模型决定调用什么，工具读取或改变外部状态，运行框架负责把结果交回模型。三者的职责不能混在一起。

一个叫“库存查询”的工具，不会因为名称正确就自动可靠。它还需要说明项目、料号、数据时间、单位、失败情况和返回范围。模型选对工具，只是第一步。

本章的五类工具可按用途理解：感知获取信息，执行改变状态，协作分配任务，沟通向用户传递信息，事件触发让外部事件唤醒任务。发送通知和触发任务不是一回事。

## 工具形态与披露数量是两个决定

“专用函数还是通用执行器”决定如何表达能力；“一次展示全部还是按需发现”决定模型眼前放多少能力。

专用函数适合需要严格参数、权限和审计的操作。通用代码适合批量加工数据、计算和组合现有能力。Skill 可以描述流程，但文档本身不是执行权限，也不会自动完成参数校验。

所以，工具少不一定能力弱；工具多也不一定更容易完成任务。关键是任务需要的能力能否被找到、参数能否传对、结果能否验证。

## 实验：123 个工具全部展示，还是按需发现

本次使用 DeepSeek API 和 123 个合成工具定义：库存、采购交期、汇率三个工具，另有 120 个无关定义。测试任务固定为计算 MAT-731 的库存可用天数和缺口。

两组各跑三次。全量组从第一轮就能看到全部 schema；发现组先看到发现入口，找到相关定义后，在下一次请求中真正加入可调用 tools 字段。

| 观察项 | 全量展示 | 按需发现 |
|---|---:|---:|
| 正确完成库存任务 | 3/3 | 3/3 |
| 总模型请求数 | 6 | 9 |
| 累计 prompt_tokens | 50,727 | 5,438 |
| 累计请求耗时 | 12.01 秒 | 13.57 秒 |

两组都得到 18÷6=3 天、7−3=4 天。按需发现减少了约 89.3% 的累计输入 tokens，但增加了调用轮数，本次没有更快，也没有证明准确率提高。

边界：这是同一合成任务的三次重复，发现器使用关键词路由和固定后备候选，不是语义向量搜索；不是原书 Qwen3-4B、真实跨领域任务与 127 个 MCP 工具的复现。重复同一道题也不等于三个独立任务。

## 实验：真实 MCP 协议与文件边界

使用官方 Python MCP SDK 1.30.0，客户端通过 stdio 启动本地服务，完成初始化、工具列表和六次真实调用：

- 写入合法 Python 文件，返回文件哈希。
- 限量读取文件，返回截断标志与原始长度。
- 提交语法错误代码，返回行号并拒绝写入。
- 请求读取允许目录之外的文件，返回错误。
- 建立人工审核请求，状态为 pending。
- 查询审核状态，仍为 pending，没有擅自变成已批准。

MCP 提供标准连接和调用方式，但业务权限、范围限制、验证逻辑仍要自己实现。通过 MCP 调用了工具，不等于这个工具自动具备安全隔离。

这里的 Python 写入只检查语法，不证明代码安全；路径检查也没有穷尽符号链接和并发替换攻击。审核队列只是本地状态演示，没有向真实用户发送通知，也没有完成真实人工审批。

## 我会怎样设计库存工具

读取类工具返回料号、项目、单位、时间与来源，限制正文长度，同时允许按页或按记录继续读取。执行类工具先依据服务端数据校验，再修改状态；不能仅相信模型提交的“我已经检查过了”。

错误也应可操作。例如“缺少 project_id”比“调用失败”更有用；“库存记录不存在”不能被当成“库存为零”。对于重复提交，执行端应考虑幂等键和操作记录，避免断线重试造成重复写入。本次没有测试真实支付、邮件或生产数据库操作。

## 这章的收获

我最关注的已经不是“给 Agent 再加几个工具”，而是让现有工具形成清楚的契约：什么时候用、输入是什么、结果意味着什么、失败后怎么办、哪些状态不能改变。

本次没有执行完整的多模态三路线对比，也没有验证真实邮箱、日历、外部通知等服务。原书的实验记录属于作者的证据，不会直接当作我本机的结果。

参考：[原书实验入口](https://github.com/bojieli/ai-agent-book/blob/main/chapter4/README.md)、[原书验收记录](https://github.com/bojieli/ai-agent-book/blob/main/chapter4/EXPERIMENT_LEDGER.md)。
